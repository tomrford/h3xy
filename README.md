# H3xy

H3xy is a Vector HexView drop-in replacement CLI for the supported non-proprietary workflows, with a matching Rust library built around `HexFile`, `Segment`, `AddressRange`, and per-operation APIs.

This was built with a Ralph loop running tests against the original binary via WSL.
